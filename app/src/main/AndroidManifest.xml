<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appanotacoes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Anotações"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppAnotacoes">
        <activity
            android:name=".activitys.EditarAnotacaoActivity"
            android:exported="true" />
        <activity
            android:name=".activitys.CriarAnotacaoActivity"
            android:exported="true"
            android:label="@string/title_activity_criar_anotacao"
            android:theme="@style/Theme.AppAnotacoes.NoActionBar" />
        <activity
            android:name=".activitys.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>